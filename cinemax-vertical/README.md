# CineMax Vertical 🎬📱

เว็บดูซีรีส์สั้น**แนวตั้ง**สไตล์จีน — **เวอร์ชันใช้งานจริง** เชื่อมต่อ Supabase + เล่นวิดีโอจริง
รองรับทุกอุปกรณ์: มือถือ / iPad / คอมพิวเตอร์

## ใช้งานได้จริงยังไง

- **ข้อมูลหนัง/ซีรีส์** โหลดจาก Supabase ตาราง `movies` (โปรเจกต์เดียวกับเว็บเดิม — เพิ่ม/แก้หนังจากหลังบ้านเว็บเดิมได้เลย ข้อมูลจะมาแสดงที่นี่อัตโนมัติ)
- **ตอนซีรีส์** โหลดจากตาราง `episodes` (season / episode / video_url)
- **เล่นวิดีโอจริง** รองรับ: Cloudflare Stream, YouTube, ไฟล์ HLS (.m3u8 เช่น Bunny), MP4 ตรง, Google Drive
- **ดูต่อจากที่ค้าง** จำตอน + วินาทีที่ดูค้าง (localStorage) เปิดมาดูต่อได้เลย
- **รายการโปรด / ถูกใจ** เก็บในเครื่องผู้ใช้
- **แชร์** ลิงก์เปิดตรงเข้าเรื่องนั้น (`/#movieId`)
- **โหมดตัวอย่าง** ถ้าต่อฐานข้อมูลไม่ได้ จะแสดงข้อมูลจำลองพร้อมแบนเนอร์เตือน (เว็บไม่ล่ม)

## ⚡ ติดตั้งระบบยอดวิว + คอมเมนต์ (ทำครั้งเดียว)

เปิด **Supabase Dashboard → SQL Editor** → วางเนื้อหาไฟล์ `sql/setup.sql` ทั้งไฟล์ → กด **Run**
จะได้ตาราง `stats` (ยอดวิว/ถูกใจ) และ `comments` พร้อม RLS ที่ปลอดภัย
(ผู้ชมเพิ่มยอดได้ทีละ +1 ผ่านฟังก์ชันเท่านั้น ปลอมตัวเลขไม่ได้ / คอมเมนต์ลบได้จากหลังบ้านเท่านั้น)
ถ้ายังไม่รัน SQL เว็บก็ยังทำงานปกติ แค่ไม่แสดงยอดวิวจริงและส่งคอมเมนต์ไม่ได้

## ระบบยอดวิว/ถูกใจจริง

- เปิดดูเรื่องไหน นับวิวจริงลง Supabase (ครั้งเดียวต่อเรื่องต่อการเปิดเว็บ)
- กดถูกใจ/เลิกถูกใจ อัปเดตยอดจริง — ตัวเลขบนปุ่มหัวใจคือยอดรวมของผู้ชมทุกคน
- **อันดับยอดฮิต + Billboard เรียงตามยอดวิวจริง** (ถ้ายังไม่มียอด ใช้คะแนน rating แทน)
- ยอดวิวแสดงบนโปสเตอร์แบบ 👁 1.2M เหมือนแอปจีน

## ระบบคอมเมนต์ใต้ตอน

- ปุ่ม 💬 ในแถบขวาของตัวเล่น → bottom sheet คอมเมนต์แบบ TikTok
- ใช้ชื่อเล่น ไม่ต้องล็อกอิน (จำชื่อไว้ในเครื่อง) · แท็ก EP อัตโนมัติเมื่อคอมเมนต์จากตอนซีรีส์
- ลบ/จัดการคอมเมนต์ได้จาก Supabase Dashboard → Table Editor → comments

## โครงสร้างไฟล์

```
cinemax-vertical/
├── index.html          # โครง HTML ทุกหน้า
├── css/styles.css      # สไตล์ + responsive ทุกขนาดจอ
├── manifest.webmanifest # PWA manifest
├── service-worker.js    # PWA: cache app shell + ออฟไลน์
├── icons/               # ไอคอนแอป (192/512/maskable/apple-touch)
├── sql/setup.sql        # ★ รันใน Supabase SQL Editor ครั้งเดียว (ยอดวิว+คอมเมนต์)
└── js/                  # โหลดเรียงลำดับ 01→12 (ห้ามสลับ)
    ├── 01-icons.js         # ไอคอน SVG
    ├── 02-config.js        # ★ ค่าเชื่อมต่อ Supabase (แก้ที่นี่ถ้าเปลี่ยนโปรเจกต์)
    ├── 03-data.js          # โหลด movies/episodes + แปลงข้อมูล + localStorage
    ├── 04-stats.js         # ยอดวิว/ถูกใจจริง (ตาราง stats + RPC)
    ├── 05-render.js        # หน้าแรก / ชาร์ต / ค้นหา / โปรไฟล์
    ├── 06-nav.js           # สลับหน้า
    ├── 07-player.js        # ★ ตัวเล่นแนวตั้ง — วิเคราะห์ URL แล้วเลือกวิธีเล่น
    ├── 08-sheet.js         # bottom sheet เลือกตอน/เลือกเรื่อง
    ├── 09-comments.js      # คอมเมนต์ใต้ตอน (ตาราง comments)
    ├── 10-boot.js          # โหลดข้อมูลตอนเปิดเว็บ + เปิดจากลิงก์แชร์
    ├── 11-pwa.js           # ลงทะเบียน service worker + ปุ่มติดตั้งแอป
    └── 12-hover.js         # เล่นตัวอย่างตอน hover (คอม)
```

## UX เหมือนแอปจริง

- **ปุ่มย้อนกลับ (Android/เบราว์เซอร์)** ทำงานเป็นลำดับ: ปิดคอมเมนต์ → ปิดหน้าต่างตอน → ปิดตัวเล่น → กลับแท็บก่อนหน้า → ออกจากเว็บ (ไม่หลุดออกจากเว็บกลางคัน)
- **โหลดแบบ skeleton** โปสเตอร์ shimmer ระหว่างรอข้อมูล ไม่มีจอขาว
- **จำตำแหน่ง scroll** ของแต่ละแท็บ สลับไปมาไม่เด้งขึ้นบนสุด
- **ปุ่ม "ลองใหม่"** บนแบนเนอร์เมื่อเชื่อมต่อฐานข้อมูลไม่ได้
- **ปุ่ม "ดูเพิ่ม"** ใต้กริดเมื่อมีหนังมากกว่าที่แสดง
- **Spinner ขณะบัฟเฟอร์** + **บอกเวลา (1:23 / 45:00) ขณะลากแถบเวลา**
- **ปุ่มลูกศรเปลี่ยนตอน** ข้างจอบนคอม (เสริมจากคีย์ ↑↓)
- **หน้าต่างเลือกตอนมีภาพพรีวิว** ทุกตอน (ใช้ field `thumbnail` จากตาราง episodes — ถ้าไม่มีใช้สีพื้นแทน) พร้อมชื่อตอน ความยาว เครื่องหมาย ✓ ตอนที่ดูแล้ว
- **ปุ่มเล่นกลางเมนูล่าง** วงกลมเรืองแสง pulse + ป้าย "เล่นเลย" — กดแล้วสุ่มเปิดเรื่องทันที

## ฟีเจอร์ตัวเล่น (เฉพาะทางสำหรับซีรีส์แนวตั้ง)

- **แตะ 1 ครั้ง** หยุด/เล่น · **แตะ 2 ครั้ง** กดถูกใจ (มีหัวใจเด้งตรงจุดที่แตะ)
- **กดค้าง** เร่งภาพ 2x (ปล่อยกลับความเร็วเดิม) — แบบเดียวกับ TikTok/ReelShort
- **ปุ่มปรับความเร็ว** 1x → 1.25x → 1.5x → 2x (จำค่าไว้)
- **ลากแถบเวลา** เพื่อเลื่อนไปยังจุดที่ต้องการ (วิดีโอ HLS/MP4)
- **จำเสียง/ความเร็ว** ครั้งต่อไปเปิดมาใช้ค่าเดิม
- **เล่นจบตอนอัตโนมัติ** เลื่อนไปตอนถัดไปเอง + จำวินาทีที่ดูค้าง
- **คีย์ลัดบนคอม**: ↑↓ เปลี่ยนตอน · Space หยุด/เล่น · M เสียง · Esc ปิด

## หน้าแรกแบบ Billboard

Hero เปลี่ยนเป็นภาพใหญ่เต็มความกว้างแบบ Netflix — หมุนอัตโนมัติทุก 6 วินาที,
มีแถบโปสเตอร์เลือกเรื่อง (บนแท็บเล็ต/คอม), จุดบอกตำแหน่ง, ปัดเปลี่ยนได้บนมือถือ,
ปุ่ม "ดูเลย" + "รายการโปรด" ในตัว

## 📲 PWA — ติดตั้งเป็นแอป

เว็บติดตั้งลงหน้าจอโฮมได้เหมือนแอปจริง (เปิดเต็มจอ ไม่มีแถบเบราว์เซอร์ + โหลดเร็ว + เปิดได้แม้เน็ตหลุด)

- **Android/Chrome/Edge**: เด้งแบนเนอร์ "ติดตั้ง CineMax" อัตโนมัติ หรือกดที่ ของฉัน → ติดตั้งเป็นแอป
- **iPhone/Safari**: กดปุ่ม → มีวิธีบอกทีละขั้น (แชร์ → เพิ่มไปยังหน้าจอโฮม)
- **ออฟไลน์**: หน้าเว็บ/สไตล์/สคริปต์ถูก cache ไว้ เปิดได้แม้เน็ตหลุด (ตัววิดีโอกับข้อมูลยังต้องใช้เน็ต)

> ⚠️ PWA + Service Worker ทำงานเมื่อเปิดผ่าน **https://** (หรือ localhost) เท่านั้น
> เปิดไฟล์ตรงๆ แบบ `file://` จะไม่ติดตั้ง — ต้อง deploy ขึ้นโฮสต์/GitHub Pages ก่อน
> เมื่ออัปเดตไฟล์ ให้แก้เลขเวอร์ชันใน `service-worker.js` (`var CACHE = 'cinemax-v1'` → `v2`) เพื่อล้าง cache เก่า

## 🖱️ เล่นตัวอย่างตอน hover (คอม)

ชี้เมาส์ค้างที่โปสเตอร์ ~0.7 วินาที จะเล่นตัวอย่างเงียบๆ ในโปสเตอร์ (ใช้ field `trailer` ก่อน ถ้าไม่มีใช้วิดีโอหลัก) — เฉพาะบนคอมที่ใช้เมาส์

## Responsive 3 ระดับ

| จอ | การแสดงผล |
|---|---|
| มือถือ (≤740px) | เต็มจอ, เมนูล่าง 5 ปุ่ม, กริด 3 คอลัมน์ |
| iPad (741–1100px) | กริดอัตโนมัติ 4–5 คอลัมน์, hero 2 ใบ, เมนูล่างเหมือนมือถือ |
| คอม (≥1101px) | เมนูบน (ไม่มีเมนูล่าง), กริด 6–7 คอลัมน์, player เป็นคอลัมน์ 9:16 กลางจอ + ปุ่มด้านขวา, คีย์ลัด: ↑↓ เปลี่ยนตอน · Space หยุด/เล่น · M ปิด/เปิดเสียง · Esc ปิด |

## กติกาเลือกวิธีเล่น (js/06-player.js → classify)

| URL | วิธีเล่น |
|---|---|
| `*.m3u8` (Bunny, CF HLS) | native `<video>` + hls.js — ควบคุมเต็ม, จำวินาทีที่ค้าง, จบตอนแล้วเลื่อนตอนถัดไปอัตโนมัติ |
| `*.mp4 / .webm / .mov` | native `<video>` เช่นกัน |
| `cloudflarestream.com / videodelivery.net` | iframe embed ของ Cloudflare |
| YouTube ทุกรูปแบบ (watch/shorts/youtu.be) | iframe youtube-nocookie |
| Google Drive | iframe `/preview` |

> 💡 **แนะนำ:** ถ้าอยากได้ประสบการณ์แนวตั้งเต็มรูปแบบ (จำวินาที + auto-next + tap หยุด/เล่น)
> ให้ใส่ลิงก์แบบ **HLS `.m3u8`** ของ Cloudflare Stream (`https://customer-XXXX.cloudflarestream.com/VIDEO_ID/manifest/video.m3u8`)
> แทนลิงก์ iframe — หาได้จาก Cloudflare Dashboard → Stream → วิดีโอ → HLS Manifest URL

## การ Deploy

เป็น static site ล้วน ไม่ต้อง build — อัปโหลดขึ้นที่ไหนก็ได้:

- **โฮสต์เดิม (Apache)**: อัปทั้งโฟลเดอร์ขึ้น web root หรือโฟลเดอร์ย่อย เปิดได้ทันที
- **GitHub Pages**: push ขึ้น repo → Settings → Pages → เลือก branch → เสร็จ
- ไม่ต้องใช้ PHP (เวอร์ชันนี้อ่านอย่างเดียว ไม่มีหลังบ้านในตัว)

### สิ่งที่ต้องตรวจฝั่ง Cloudflare Stream

ถ้าวิดีโอไม่เล่น ให้เช็ก **Allowed Origins** ของ Stream:
Cloudflare Dashboard → Stream → Settings → Allowed Origins — ต้องมีโดเมนใหม่ที่ deploy (เช่น `romazeed1.info`, `*.github.io`) หรือปล่อยว่าง (อนุญาตทุกโดเมน)

### สิ่งที่ต้องตรวจฝั่ง Supabase

- ตาราง `movies` และ `episodes` ต้องเปิด RLS ให้ **anon อ่านได้** (SELECT) — ถ้าเว็บเดิมใช้งานได้อยู่แล้ว แปลว่าตั้งไว้ถูกแล้ว
- key ที่อยู่ใน `02-config.js` เป็น anon key (public read) เปิดเผยได้ตามดีไซน์ของ Supabase — **ห้าม**เอา service_role key มาใส่เด็ดขาด

## การจัดการเนื้อหา

เวอร์ชันนี้เป็น "หน้าเว็บผู้ชม" — เพิ่ม/แก้หนัง, อัปโหลดวิดีโอขึ้น Cloudflare, จัดการตอน
ให้ทำผ่าน**หลังบ้านของเว็บเดิม**ตามปกติ ข้อมูลจะแสดงที่นี่อัตโนมัติ (แชร์ฐานข้อมูลเดียวกัน)
